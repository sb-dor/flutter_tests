import 'package:flutter/material.dart';

// for web running
void run() => runApp(
      MaterialApp(
        home: Scaffold(
          body: Center(
            child: Text("WEB"),
          ),
        ),
      ),
    );
